name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    name: Python ${{ matrix.python-version }}
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12"]

    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@0b93645e9fea7318ecaed2b359559ac225c90a2b  # v5.3.0
        with:
          python-version: ${{ matrix.python-version }}
          cache: 'pip'

      - name: Install (bare — no torch, no GPU)
        run: pip install -e .

      - name: Lint (ruff)
        run: |
          # Pinned deliberately. [tool.ruff] in pyproject sets no `select`, so the
          # active rule set is whatever the installed version defaults to — an
          # unpinned install turns every ruff release into a surprise CI failure on
          # untouched code. 0.16.4 flags 36 pre-existing violations (I001, UP045,
          # C408, BLE001, S110); clearing those is its own change. Bump this pin in
          # the same PR that clears them.
          pip install ruff==0.15.17
          ruff check harness/ tests/

      - name: Import smoke test
        run: |
          python -c "from harness import scorers, judge, run_eval, models, stats"

      - name: CLI smoke test
        run: python -m harness.run_eval --help

      - name: Run tests (if present)
        run: |
          if find . \( -name "test_*.py" -o -name "*_test.py" \) | grep -q .; then
            pip install pytest
            python -m pytest -q
          else
            echo "No tests found — skipping pytest"
          fi
