# Dependencies are defined in pyproject.toml — install with:
#   pip install git+https://github.com/getsimpledirect/cblre.git          # core
#   pip install "cblre[vertex] @ git+https://github.com/getsimpledirect/cblre.git"  # + Vertex AI
#   pip install "cblre[local] @ git+https://github.com/getsimpledirect/cblre.git"   # + HF local
# or, in a local checkout:
#   pip install -e .            # core deps
#   pip install -e ".[vertex]"  # + Vertex AI judge path
#   pip install -e ".[local]"   # + local HuggingFace checkpoint inference
