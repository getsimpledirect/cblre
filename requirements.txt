# Core — required for OpenAI-compatible inference endpoints (OpenAICompatClient)
requests

# LLM-as-judge — the canonical judge is Claude Sonnet 4.6 via the native
# Anthropic API (AnthropicClient; set ANTHROPIC_API_KEY).
# Teams on GCP can reach the same judge through Vertex AI instead:
#   pip install "anthropic[vertex]"   (VertexAnthropicClient, ADC auth)
anthropic

# ── Optional: local HuggingFace inference only (HFLocalClient) ───────────────
# Uncomment if you are loading a local transformers checkpoint
# torch
# transformers
# peft
